<Window x:Class="Lesson_7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lesson_7"
        mc:Ignorable="d"
        Title="Three-State CheckBox Demo" Height="250" Width="400">

    <Window.Resources>
        <ControlTemplate x:Key="CustomCheckBoxTemplate" TargetType="CheckBox">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"                      
                      HorizontalAlignment="Center"
                      Margin="0,0,0,5">
                    <Rectangle x:Name="checkBoxRect" 
                              Width="20" Height="20" 
                              RadiusX="3" RadiusY="3"
                              Stroke="#FF707070" 
                              StrokeThickness="1.5"
                              Fill="White"/>
                    <TextBlock x:Name="checkMark" 
                              Text="X" 
                              FontWeight="Bold"
                              FontSize="12"
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center" 
                              Opacity="0"/>
                    <Ellipse x:Name="indeterminateMark" 
                            Width="8" Height="8" 
                            Fill="#FF707070" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Opacity="0"/>
                </Grid>
                <ContentPresenter Grid.Row="1" 
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
            </Grid>

            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="checkMark" Property="Opacity" Value="1"/>
                    <Setter TargetName="checkBoxRect" Property="Fill" Value="#FFE6E6E6"/>
                </Trigger>

                <Trigger Property="IsChecked" Value="{x:Null}">
                    <Setter TargetName="indeterminateMark" Property="Opacity" Value="1"/>
                    <Setter TargetName="checkBoxRect" Property="Fill" Value="#FFF0F0F0"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Column="0"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Template="{StaticResource CustomCheckBoxTemplate}" 
                  Content="Checked State" 
                  IsChecked="True"/>

        <CheckBox Grid.Column="1" 
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Template="{StaticResource CustomCheckBoxTemplate}" 
                  Content="Unchecked State" 
                  IsChecked="False"/>

        <CheckBox Grid.Column="2"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Template="{StaticResource CustomCheckBoxTemplate}" 
                  Content="Indeterminate State" 
                  IsChecked="{x:Null}"/>
    </Grid>
</Window>